import { authService } from './auth';\n\n/**\n * Initialize the super admin account if it doesn't exist\n * This should be run once during application setup\n */\nexport const initializeApp = async () => {\n  try {\n    console.log('Initializing application...');\n    \n    const result = await authService.initializeSuperAdmin();\n    \n    if (result) {\n      console.log('Super admin account created successfully');\n      console.log('Email: superadmin@office.com');\n      console.log('Password: SuperAdmin123!');\n      console.log('IMPORTANT: Change these credentials immediately after first login!');\n    } else {\n      console.log('Super admin account already exists');\n    }\n    \n    return true;\n  } catch (error) {\n    console.error('Error initializing application:', error);\n    return false;\n  }\n};\n\n/**\n * Check if current user is super admin\n */\nexport const checkSuperAdminAccess = async (uid: string): Promise<boolean> => {\n  try {\n    return await authService.isSuperAdmin(uid);\n  } catch (error) {\n    console.error('Error checking super admin access:', error);\n    return false;\n  }\n};