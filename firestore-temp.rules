rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all operations for super admin setup
    // TODO: Replace with secure rules after super admin is created
    match /{document=**} {
      allow read, write: if true;
    }
  }
}